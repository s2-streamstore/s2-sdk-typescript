{
  "name": "s2-sdk-typescript-v2",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "check": "biome check --write && tsc --noEmit",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.5",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
