{
  "name": "@s2-dev/streamstore-patterns",
  "version": "2.0.0",
  "description": "Add-on patterns built on top of the S2 StreamStore TypeScript SDK.",
  "license": "Apache-2.0",
  "type": "module",
  "homepage": "https://s2.dev",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit",
    "format": "biome format --write",
    "lint": "biome check",
    "test": "vitest --run"
  },
  "sideEffects": false,
  "peerDependencies": {
    "@s2-dev/streamstore": ">=0.20.0"
  },
  "devDependencies": {
    "@ai-sdk/openai": "^2.0.68",
    "@msgpack/msgpack": "^3.1.2",
    "ai": "^5.0.95",
    "debug": "^4.4.3",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "nanoid": "^3.3.11"
  }
}
