# @s2-dev/streamstore-patterns

Higher-level, opinionated helpers built on top of the core [`@s2-dev/streamstore`](https://www.npmjs.com/package/@s2-dev/streamstore) SDK.

This package is **optional** – you can use `@s2-dev/streamstore` directly without ever installing or importing `@s2-dev/streamstore-patterns`.

